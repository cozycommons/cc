# eve@0.16.0 lists vite@^8 / @sveltejs/kit as optional peers (only used by eve's
# own Nuxt/SvelteKit/Vite integrations). This frontend is on vite@5 and only uses
# the `eve/react` chat hook, so skip the unmet optional-peer error during install.
legacy-peer-deps=true
